name: Frontend Check

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - 'shared/**'
  pull_request:
    paths:
      - 'frontend/**'
      - 'shared/**'
  workflow_dispatch:

# Cloudflare Pages handles deployment automatically via Git integration.
# This workflow runs type checking on push and PR.
jobs:
  check:
    runs-on: ubuntu-latest
    name: Type Check
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm --filter frontend tsc --noEmit
